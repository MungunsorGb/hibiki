// secrets.h.example - copy this file to secrets.h and fill in your real
// network credentials. secrets.h is gitignored and must never be committed.
//
//   cp secrets.h.example secrets.h
//
#pragma once

#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
